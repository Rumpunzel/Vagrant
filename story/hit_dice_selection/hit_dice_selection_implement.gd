@tool
class_name HitDiceSelectionButtonImplement
extends Control

func update_breath_dice(_available_breath_dice: Array[BreathDie]) -> void:
	assert(false, "Not implemented, needs to be overriden!")

func update_save_request(_save_request: SaveRequest) -> void:
	assert(false, "Not implemented, needs to be overriden!")

func update_save_result(_save_result: SaveResult) -> void:
	assert(false, "Not implemented, needs to be overriden!")

func select() -> void:
	assert(false, "Not implemented, needs to be overriden!")

func deselect() -> void:
	assert(false, "Not implemented, needs to be overriden!")

func disable() -> void:
	assert(false, "Not implemented, needs to be overriden!")
